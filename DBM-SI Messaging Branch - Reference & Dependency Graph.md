#DBM-SI Messaging Branch — Reference & Dependency Graph

##ROOT: Structural Messaging (DBM-SI)

    ┌──────────────────────────────────────────────────────────-─┐
    │ ITEM #266  (Possibility Result / Demonstration)            │
    │  • Claim: MET-level messaging can emerge from structural   │
    │    interpretation over correlated entropy                  │
    │  • Key property: does NOT require a controllable           │
    │    transmission channel (classic bit-pipe)                 │
    │  • Role: establishes feasibility + motivates kernel search │
    └──────────────────────────────────────────────────────────-─┘
                         │
                         │  (1) provides empirical/constructive anchor
                         │  (2) exposes missing kernel vocabulary
                         ▼
    ┌─────────────────────────────────────────────────────────-──┐
    │ ITEM #267  (Kernel Formalization / Normative Backbone)     │
    │  • Reframes: not transporting bits/structure, but pointing │
    │    into shared decoding space (CCC space)                  │
    │  • Kernel Triad: SM = (B, A, D)                            │
    │       B = Public Observable Board                          │
    │       A = Actuation Knob (minimal controllable perturbation│
    │       D = Shared Decoder Space (CCC / candidates)          │
    │  • APTGOE axes: header / pointer alphabet / decoder evol.  │
    │  • Carrier rule: correlation alone insufficient; requires A│
    │  • Minimal formal model: finite candidate evidence         │
    │    convergence to stable C*                                │
    │  • Role: turns #266 “it works” → “why it works / when it   │
    │    can work / how it evolves”                              │
    └──────────────────────────────────────────────────────────-─┘
                         │
                         │  (3) exported to spec as normative kernel
                         ▼
    ┌─────────────────────────────────────────────────────────---──┐
    │ StructuralMessaging-Spec / README (Release-ready artifacts)  │
    │  Kernel Mechanism section includes:                          │
    │   - Definition Box (SM=(B,A,D); necessary conditions)        │
    │   - Non-Normative Example Box                                │
    │   - Security/Detectability Note                              │
    │   - Implementation Guidance Note                             │
    │   - Glossary + Notation                                      │
    │   - Minimal Formal Model (defs + convergence theorem sketch) │
    └─────────────────────────────────────────────────────────---──┘
